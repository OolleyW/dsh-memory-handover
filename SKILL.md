---
name: memory-handover
description: >-
  会话记忆与交接：在会话结束前（或用户说「保存记忆 / 写交接 / 交接一下 / 记住进度 /
  接续」等）把本会话的任务、已完成、卡点、下一步计划、踩过的坑，整理写入交接文档
  Memory_DSH.md，供下一个会话开头「读 Memory_DSH.md 接续」恢复上下文。
whenToUse: >-
  用户要求保存记忆、写交接、交接文档、记住进度、会话要结束、读 Memory_DSH.md 接续；
  或你判断当前会话即将结束、上下文即将丢失、需要把进度沉淀成持久记忆时。
license: MIT
---

# Memory Handover（记忆交接）

## 目标

维护一份 `Memory_DSH.md` 交接文档，作为跨会话的持久记忆。每个会话结束前（或用户要求时）把上下文沉淀进去，让下一个会话能无缝接续。

## 目标文件

- 默认：当前工作区根目录的 `Memory_DSH.md`（例如 `E:\DSH_work\Memory_DSH.md`）。
- 若本会话 / 项目已有固定的交接文件（如用户之前一直用的 `E:\DSH_work\Memory_DSH.md`），沿用该路径，不要另建新文件。
- 文件不存在 → 创建；已存在 → **追加 / 更新，绝不覆盖或删除已有内容**。

## 每次必须记录的五件事

1. **什么时候、在做什么任务**：一句任务主线 + 关键时间线（带日期）。
2. **已经完成了什么**：写具体产物（文件路径、命令、结果数字），不要空话。
3. **当前卡在哪**：硬阻塞 / 未闭环点 / 遗留待办，逐条列出。
4. **下一步计划**：按优先级列出可执行步骤。
5. **踩过的坑（绝对不要踩）**：逐条、可执行，写清「为什么」和「怎么避免」。

## 写法要求

- 中文、Markdown。
- 新任务用 `## 【任务N】… — 交接记录` 作为分隔；更新旧任务时直接在原章节内补时间线/状态，不要另起重复章节。
- 每个新章节顶部写一行恢复方式，例如：「恢复方式：新会话开头说『读一下 Memory_DSH.md，接续 XXX』」。
- 只写事实与本会话可验证的信息；不确定的标注「待验证」。
- 结尾给一句确认：「本次已记录 / 更新到 Memory_DSH.md」。

## 流程

1. 若文件已存在，先 `read` 它，了解已有结构与最后状态。
2. 按上面五个维度整理本会话。
3. 用 `edit`（追加）或 `write`（新建）写入，保持已有内容完整、不丢历史。
4. 回读确认写入正确、中文无乱码（用 `read` 工具核对，不要用 pwsh 的 `Get-Content` 判断中文）。
